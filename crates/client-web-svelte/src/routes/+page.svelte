<script lang="ts">
  // Placeholder home. The vanilla client's homeView.ts is ~1k lines of shelf/
  // feature rendering — out of scope for this PoC. This page exists to prove
  // routing parity (the `/` route resolves) and to link into the ported view.
</script>

<section class="panel page-panel">
  <h3>Koko — Svelte 5 PoC</h3>
  <p class="muted">
    This is a minimal Svelte 5 port of the Koko web client, scoped to the Settings → Logs view.
    See <code>PROPOSAL.md</code> in the worktree root for the full migration analysis.
  </p>
  <p>
    <a href="/settings/logs">Go to Settings → Logs</a> (the ported view).
  </p>
</section>
